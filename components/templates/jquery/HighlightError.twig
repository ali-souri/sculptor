<div id="{{id}}" class="ui-widget">
	<div class="ui-state-{{type|default('highlight')}} ui-corner-all" style="margin-top: 20px; padding: 0 .7em;">
		<p><span class="ui-icon ui-icon-info" style="float: left; margin-right: .3em;"></span>
		<strong>{{strong_text|default('')}}</strong> {{text|default('Sample ui-state-highlight style.')}}</p>
	</div>
</div>